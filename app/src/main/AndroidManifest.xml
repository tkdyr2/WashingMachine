<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.washingmachine">

    <uses-permission android:name="android.permission.INTERNET"/>;

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MyInformation">
        </activity>
        <activity android:name=".MakeNotice">
        </activity>
        <activity android:name=".Registration">
        </activity>
        <activity android:name=".NoticeAnswer">
        </activity>
        <activity android:name=".MakeAnswer">
        </activity>
        <activity android:name=".LookNoticeWhole">
        </activity>
        <activity android:name=".LookNotice">
        </activity>
        <activity android:name=".LookAnswer">
        </activity>


    </application>

</manifest>